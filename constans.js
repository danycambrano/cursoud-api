const DB_USER="admin"
const DB_PASSWORD="";
const DB_HOST="ninguno"
const DB_NAME="prueba"
const DB_PORT="27017"

const API_VERSION="v1";
const IP_SERVER="localhost";

const JWT_SECRET_KEY="palabrasecreta"

module.exports={
    DB_USER,
    DB_PASSWORD,
    DB_HOST,
    DB_NAME,
    API_VERSION,
    IP_SERVER,
    DB_PORT,
    JWT_SECRET_KEY
}